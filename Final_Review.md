# Java期末考试简答题速背笔记（精简版）

### 1. 请解释 final 关键字的用法。
* **修饰类**：**不可继承**（如 `String`），该类所有方法隐式变为 `final`。
* **修饰方法**：**不可重写（Override）**，但可以被继承和重载。
* **修饰变量**：**不可修改（常量）**。
  * **基本类型**：数值不可变。
  * **引用类型**：地址不可变，但对象内部的属性可变。

---

### 2. 简述重载（Overload）与重写（Override）的区别。
* **重载（Overload）**：**同类同名**。参数列表必须不同（个数/类型/顺序）。实现**编译期多态**。
* **重写（Override）**：**父子类同名同参**。遵循“两同两小一大”原则（返回值、异常 $\le$ 父类；访问权限 $\ge$ 父类）。实现**运行期多态**。

| 区别点 | 重载 (Overload) | 重写 (Override) |
| :--- | :--- | :--- |
| **位置** | 同一个类（或子类中） | 父子类之间 |
| **方法名/参数** | 方法名相同，参数**必须不同** | 方法名和参数**必须相同** |
| **多态绑定** | 静态绑定（编译期多态） | 动态绑定（运行期多态） |

---

### 3. 请比较接口与抽象类的区别（基于Java 8/9以上版本）。
* **相同点**：都不能被实例化；都可以包含抽象方法、静态方法和默认/私有方法。
* **不同点**：
  * **继承限制**：类只能单继承抽象类，但可以多实现接口。
  * **成员变量**：抽象类可定义任意变量；接口只能定义公有静态常量（隐式 `public static final`）。
  * **构造方法**：抽象类**有**构造方法（供子类调用）；接口**没有**构造方法。
  * **核心理念**：抽象类体现 **IS-A**（代码复用）；接口体现 **LIKE-A**（行为规范）。

---

### 4. 简述 Error 与 Exception 的联系与区别。
* **联系**：都继承自 `Throwable`，均可在运行时抛出。
* **区别**：
  * **Error（错误）**：JVM 级别的严重问题。**程序无法处理**，发生时 JVM 通常选择终止线程（如 `OutOfMemoryError`、`StackOverflowError`）。
  * **Exception（异常）**：程序本身的逻辑问题。**程序可以处理**（通过 `try-catch` 捕获）。分为受检异常（编译期强制处理）和运行时异常（`RuntimeException`）。

---

### 5. 简述关键字 throw 与 throws 的用法与区别。
* **`throw`**：**动作**。用于**方法体内部**，用于显式抛出一个具体的异常对象（如 `throw new Exception();`）。
* **`throws`**：**声明**。用于**方法签名尾部**，声明该方法可能抛出的异常类型列表，将异常处理告知调用者（漏斗、推卸责任）。

---

### 6. 请列举常用的 RuntimeException 并解释其用法。
运行时异常由程序逻辑错误引起，编译时不强制捕获：
1. `NullPointerException`（空指针）：对 `null` 对象调用方法或访问属性。
2. `ArrayIndexOutOfBoundsException`（数组越界）：使用的索引超出了数组合法范围。
3. `ClassCastException`（类型转换异常）：强转了不匹配的类型。
4. `ArithmeticException`（算术异常）：出现了非法的算术运算，如整数除以 0。
5. `IllegalArgumentException`（非法参数）：传入方法的参数不符合业务逻辑。

---

### 7. 简述 String、StringBuffer、StringBuilder 的用法与区别。
* **`String`**：**不可变**，线程安全。频繁修改会大量创建新对象，效率极低。
* **`StringBuffer`**：**可变**，**线程安全**（方法带 `synchronized`）。效率适中，用于多线程环境。
* **`StringBuilder`**：**可变**，**线程不安全**。效率最高，单线程下频繁拼接字符串首选。

---

### 8. Java 8 时间操作相对旧版时间操作做了哪些改进？
1. **线程安全**：旧版 `Date/SimpleDateFormat` 线程不安全；新版 `LocalDate/LocalTime` 是**不可变类且线程安全**。
2. **直观清晰**：旧版月份从 0 开始计算（易错）；新版月份从 1 开始，符合人类直觉。
3. **职责明确**：引入了 `LocalDate`（日期）、`LocalTime`（时间）、`LocalDateTime`（日期时间）、`Instant`（时间戳），满足不同场景。
4. **格式化增强**：引入了线程安全的 `DateTimeFormatter`。

---

### 9. List、Set、Map 三个接口，存取元素时各有什么特点？
* **`List`**：**有序、可重复**。元素按插入顺序排列，可通过正整数**索引（下标）**精准存取。
* **`Set`**：**无序、不可重复**。基于 `hashCode()` 和 `equals()` 保证唯一性。不能通过索引访问，只能遍历。
* **`Map`**：**键值对（Key-Value）存储**。**Key 必须唯一，Value 可重复**。存取时根据 Key 检索 Value，若 Key 重复会覆盖旧 Value。

---

### 10. 简述 Comparable 与 Comparator 接口的用法与区别。
* **`Comparable`（内部比较器）**：来自 `java.lang`。由**类自身实现**并重写 `compareTo(T o)` 方法。属于“侵入式”的默认排序规则。
* **`Comparator`（外部比较器）**：来自 `java.util`。单独定义**第三方解耦类**并重写 `compare(T o1, T o2)` 方法。属于策略模式，适合源码不可修改或需要多种排序规则的场景。

---

### 11. 简述线程调度的方式及其用法。
Java 采用**抢占式线程调度**模型：高优先级的线程有更高概率抢占 CPU 时间片。
* **`setPriority(int)`**：设置线程优先级（1-10），数值越大，获得 CPU 的概率越高。
* **`Thread.sleep(ms)`**：静态方法。线程进入阻塞状态，**释放 CPU 资源，但不释放锁**。
* **`Thread.yield()`**：静态方法。当前线程**让步**，从运行态转为就绪态，暗示 CPU 重新调度（可能再次选中自己）。
* **`join()`**：实例方法。当前线程挂起，等待被调用的**目标线程执行完毕**后再恢复执行（串行化）。

---

### 12. 多线程中为什么要引入同步机制？请解释 synchronized 关键字的用法。
* **引入原因**：多个线程同时读写共享的互斥资源时，由于线程切换的随机性，会导致**数据不一致（线程安全问题）**。
* **`synchronized` 用法（原子性内置锁）**：
  1. **修饰实例方法**：锁住当前**实例对象（this）**。
  2. **修饰静态方法**：锁住当前**类的 Class 对象**（全局唯一）。
  3. **修饰代码块**：显式锁住 `synchronized(obj)` **括号内的指定对象**，粒度更小。

---

### 13. 简述 sleep() 方法与 wait() 方法的区别。
1. **所属类不同**：`sleep()` 属于 `Thread` 类；`wait()` 属于 `Object` 类。
2. **锁的释放不同（核心）**：`sleep()` **不释放锁**（带着锁睡觉）；`wait()` **释放锁**（让出锁资源供他人竞争）。
3. **运行环境不同**：`sleep()` 可在任何地方使用；`wait()` 必须在 **`synchronized` 同步环境**内使用。
4. **唤醒机制不同**：`sleep()` 到时自动唤醒；无参的 `wait()` 必须等待其他线程调用 `notify()/notifyAll()` 唤醒。

---

### 14. 简述 synchronized 与 Lock 的用法与区别。
* **`synchronized`**：Java **关键字**。属于 JVM 层面的内置锁；**自动加锁与释放锁**（代码块结束或异常时）；不支持中断、不支持超时。
* **`Lock`**：Java **接口**（如 `ReentrantLock`）。属于代码层面的显式锁；**必须手动 lock() 和在 finally 块中 unlock()**；支持中断响应、支持超时尝试（`tryLock`）、支持公平锁。

---

### 15. 简述字节流与字符流的用法与区别。
* **字节流（InputStream / OutputStream）**：以 **字节 (8bit)** 为基本单位。用于处理**所有类型文件**（文本、图片、音视频、exe等）。处理多字节字符（如中文）时易乱码。
* **字符流（Reader / Writer）**：以 **字符 (char)** 为基本单位。专门处理**纯文本文件**。内部集成了编码表，能自动处理字符转换，有效避免中文乱码。**不能**处理二进制文件（图片、视频）。

---

### 16. 试比较 Statement 对象与 PreparedStatement 对象的区别。
1. **执行机制（预编译）**：`Statement` 直接发送静态 SQL，每次都需要数据库编译解析；`PreparedStatement` 支持 **SQL 预编译**，对带 `?` 占位符的 SQL 骨架只编译一次，多次执行仅传参，性能更高。
2. **安全性（SQL注入）**：`Statement` 靠字符串拼接参数，**极易受到 SQL 注入攻击**；`PreparedStatement` 使用占位符，传入参数会被视为纯字面量进行转义，**彻底杜绝 SQL 注入**。
3. **可读性**：`PreparedStatement` 避免了复杂的单双引号拼接，代码结构清晰，易于维护。